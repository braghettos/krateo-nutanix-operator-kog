# GENERATED by gen-portal-widgets.cjs from ../compositiondefinition.yaml — do not edit by hand.
apiVersion: widgets.templates.krateo.io/v1beta1
kind: Card
metadata:
  name: nutanix-virtualmachine
  namespace: krateo-system
  labels:
    krateo.io/portal-page: blueprints
    krateo.io/category: Compute
spec:
  apiRef:
    name: nutanix-virtualmachine-card-data
    namespace: krateo-system
  widgetData:
    title: Nutanix Virtualmachine
    headerLeft: Provision the Nutanix Virtualmachine blueprint.
    tooltip: Nutanix Virtualmachine blueprint
    icon:
      name: fa-server
      color: grey
    tags:
      - Compute
    extra: ''
    items: []
    footer:
      - resourceRefId: nutanix-virtualmachine-install
  widgetDataTemplate:
    - forPath: extra
      expression: '${ "Ready: " + .ready }'
    - forPath: tags
      expression: ${ ["Compute", .version] }
    - forPath: icon
      expression: '${ { name: "fa-server", color: (if .ready == "True" then "green" elif .ready == "False" then "orange" else "grey" end) } }'
  resourcesRefs:
    items:
      - id: nutanix-virtualmachine-install
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: nutanix-virtualmachine-install
        namespace: krateo-system
        resource: buttons
        verb: GET

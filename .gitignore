.DS_Store
oas/_raw/
*.tmp

# Vendored Helm subchart tarballs — reproducible from each blueprint's committed
# Chart.lock via `helm dependency build`; the .tgz itself is a build artifact.
blueprints/*/charts/

apiVersion: v1
kind: Service
metadata:
  name: {{ include "nutanix-v4-proxy.fullname" . }}
  labels:
    {{- include "nutanix-v4-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nutanix-v4-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

# Quickstart — Create a Nutanix VM

This has moved to a complete, KOG-operator-based walkthrough (with Prism Central screenshots and the Nutanix v4 middleware):

➡ **[`quickstart/README.md`](quickstart/README.md)**

It covers: deploying the translating middleware, the three OpenAPI-slice fixes the controller needs, applying the `Vm` RestDefinition, creating the `VmConfiguration` + `Vm` custom resources, and verifying the VM in Prism Central.
